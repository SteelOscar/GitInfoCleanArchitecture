<resources>

    <!-- Base application theme. -->
    <style name="StartTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="StartTheme.Launcher" parent="StartTheme">
        <item name="android:windowBackground">@drawable/splash_layout</item>
    </style>

    <style name="MainTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/gray2</item>
        <item name="colorPrimaryDark">@color/gray2</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="StartEditText" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/lite_gray</item>
    </style>

</resources>
